machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t cassandra-2.1.4 .
    - docker build -t cassandra-2.1.3 cassandra-2.1.3/
    - docker build -t cassandra-2.1.2 cassandra-2.1.2/
    - docker build -t cassandra-2.1.1 cassandra-2.1.1/
    - docker build -t cassandra-2.1.1 cassandra-2.1.0/
    - docker build -t cassandra-2.0.14 cassandra-2.0.14/
    - docker build -t cassandra-2.0.13 cassandra-2.0.13/
    - docker build -t cassandra-2.0.12 cassandra-2.0.12/
    - docker build -t cassandra-2.0.11 cassandra-2.0.11/
    - docker build -t cassandra-2.0.10 cassandra-2.0.10/
    - docker build -t cassandra-2.0.9 cassandra-2.0.9/
    - docker build -t cassandra-2.0.8 cassandra-2.0.8/
    - docker build -t cassandra-2.0.7 cassandra-2.0.7/
    - docker build -t cassandra-2.0.6 cassandra-2.0.6/
    - docker build -t cassandra-2.0.5 cassandra-2.0.5/
    - docker build -t cassandra-2.0.4 cassandra-2.0.4/
    - docker build -t cassandra-2.0.3 cassandra-2.0.3/
test:
  override:
    - docker run --name cassandra-2.1.4 -dt cassandra-2.1.4
    - docker run --name cassandra-2.1.3 -dt cassandra-2.1.3
    - docker run --name cassandra-2.1.2 -dt cassandra-2.1.2
    - docker run --name cassandra-2.1.1 -dt cassandra-2.1.1
    - docker run --name cassandra-2.1.1 -dt cassandra-2.1.0
    - docker run --name cassandra-2.0.14 -dt cassandra-2.0.14
    - docker run --name cassandra-2.0.13 -dt cassandra-2.0.13
    - docker run --name cassandra-2.0.12 -dt cassandra-2.0.12
    - docker run --name cassandra-2.0.11 -dt cassandra-2.0.11
    - docker run --name cassandra-2.0.10 -dt cassandra-2.0.10
    - docker run --name cassandra-2.0.9 -dt cassandra-2.0.9
    - docker run --name cassandra-2.0.8 -dt cassandra-2.0.8
    - docker run --name cassandra-2.0.7 -dt cassandra-2.0.7
    - docker run --name cassandra-2.0.6 -dt cassandra-2.0.6
    - docker run --name cassandra-2.0.5 -dt cassandra-2.0.5
    - docker run --name cassandra-2.0.4 -dt cassandra-2.0.4
    - docker run --name cassandra-2.0.3 -dt cassandra-2.0.3


