machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t cassandra-2.1.4 .
test:
  override:
    - docker run --name cassandra-2.1.4 -p 8080:8080 -dt cassandra-2.1.4; sleep 10
    - curl --retry 10 --retry-delay 5 -L -I -f http://localhost:8080/

